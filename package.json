{
  "name": "anmo",
  "version": "1.0.0",
  "private": true,
  "description": "An advanced anime watching application that works entirely via custom JSON sources, inspired by Tachiyomi, with a sophisticated built-in video player.",
  "scripts": {
    "dev": "echo \"Development server not configured. Open index.html in a browser.\" && exit 0",
    "build": "echo \"Build script not configured.\" && exit 1",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.4.5"
  }
}
